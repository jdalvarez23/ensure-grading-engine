/**
 * @fileOverview ensure's library for immediate front-end feedback.
 * @name intro.js<js>
 * @author Jose D. Alvarez
 * @license GPLv3
 */

/**
 * Exposes ensureFEGradingEngine (Grading Engine) interface
 * @return {Object} exports - the functions on the exports object
 */
;
window.ensureFEGradingEngine ? window.ensureFEGradingEngine = window.ensureFEGradingEngine : window.ensureFEGradingEngine = (function(window, undefined) {
      'use strict';
      var exports = {};

      // intro.js<js> ends here
